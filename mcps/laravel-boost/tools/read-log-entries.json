{
  "name": "read-log-entries",
  "description": "Read the last N log entries from the application log, correctly handling multi-line PSR-3 formatted logs and JSON-formatted logs. Only works for log files.",
  "inputSchema": {
    "properties": {
      "entries": {
        "description": "Number of log entries to return.",
        "type": "integer"
      }
    },
    "type": "object",
    "required": [
      "entries"
    ]
  }
}